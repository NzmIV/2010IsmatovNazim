using System;

namespace ConsoleApp3
{
    class Program
    {  
        public static int Fib(uint n)
        {
        
        }
        static void Main(string[] args)
        {
            double b;
            b= ((1 + Math.Sqrt(5)) / 2);
            Console.WriteLine(" b = " + ((1 + Math.Sqrt(5)) / 2));
            int n;
            uint un;
            Math.Pow(b, n);
            Math.Pow(-b, -n);
            un = Console.WriteLine((Math.Pow(b, n) - Math.Pow((-b), - n))/ 2b - 1);
            



            
            /*
            Random random = new Random();
           
            int n;

            n = random.Next('a' , 'z') ;
            Console.WriteLine((char)n);
            Console.ReadLine();
        */
            }
    }
}
